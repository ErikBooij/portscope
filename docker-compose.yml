services:
  portscope:
    build: .
    ports:
      - "127.0.0.1:8090:8090"
    volumes:
      - ./portscope.json:/app/portscope.json
      - ./.portscope:/app/.portscope
    restart: unless-stopped
